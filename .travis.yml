language: cpp

dist: bionic
addons:
  apt:
    packages:
      - libmpg123-dev
      - libsndfile-dev
      - libsdl2-dev
      - libuv1-dev
      
compiler:
  - clang
  - gcc

before_script:
  - mkdir cbuild && cd cbuild
  - cmake ..

script:
  - make
  - make check
