language: cpp

dist: bionic
addons:
  apt:
    packages:
      - libmpg123-dev
      - libsndfile-dev
      - libsdl2-dev
      - libuv1-dev
      
compiler:
  - clang
  - gcc

script:
  - mkdir cbuild && cd cbuild; cmake .. && make && make check
